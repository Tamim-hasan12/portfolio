Tamim Hasan — Static Portfolio

Quick setup

1. Place your resume PDF at `assets/Tamim10.pdf` (you provided it as Tamim10.pdf).
2. Add a profile photo as `assets/profile.jpg` (optional).
3. Open `index.html` in the browser.

Notes

- The contact form uses `mailto:` as a fallback. To use a server endpoint, replace the form `action`.
- The skills section includes Playwright and k6 as requested.
